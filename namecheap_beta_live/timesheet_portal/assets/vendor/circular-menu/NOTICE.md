# circular-menu vendor notice

MERDPOS DEV UI Studio vendors `circular-menu` version 1.0.6 from:

https://github.com/yandongCoder/circular-menu

Upstream author: yandong / yandongCoder. Upstream `package.json` declares the package license as ISC.

The vendored JavaScript and CSS are used only by the DEV-only UI Studio compact mobile action wheel. MERDPOS UI Studio styling overrides the upstream presentation so it follows MERDPOS tokens and accessibility conventions.
