version: 1
id: MERD-20260904-drupal-working-now-adapter
branch: beta/drupal-webapp
base_head: 7315be89ab1d940b20acbcf70598d6ea60e5056a
last_seen_head: 4dcaa2c1f5223f2d1c7f15c930d35ea0c076236f
objective: Wire the first read-only Drupal adapter to canonical MERDPOS Working Now data without direct operational DB access or device-token reuse.
packet_state: VALIDATED
lifecycle: WIRED
constraints:
  - Drupal must not query MERDPOS operational tables directly.
  - Device/POS credentials must not be reused as Drupal service identity.
  - MERDPOS backend role/LOA and named-permission enforcement remains authoritative.
  - Shared service secrets must be environment-only and excluded from Git.
  - No Namecheap or authoritative Beta deployment occurs in this milestone.
implementation_plan:
  - Add a narrow HMAC-authenticated GET integration endpoint beside the MERDPOS backend.
  - Resolve the requested actor and current client role/LOA from SQL.
  - Require dashboard.view plus dashboard.widget.working_now using the canonical dashboard-scoped dependency model; do not grant the broader Workforce area.
  - Return canonical merd_working_now() rows only after backend authorization.
  - Add a Drupal HTTP provider that signs requests and validates/normalizes responses.
  - Render count and roster with explicit unconfigured/error states and no fabricated data.
  - Exercise the full Drupal path against a local signature-validating fake upstream.
completed:
  - Signed Drupal HTTP provider and backend Working Now bridge implemented.
  - Backend HMAC freshness, dashboard-widget LOA parity, and fail-closed permission lookup validated.
  - Signed fake-upstream desktop/mobile browser path passed and was visually inspected.
  - Fake upstream removed; normal localhost resting state verified UNCONFIGURED with no AUTOTEST roster.
remaining:
  - Commit/push source and archive packet.
  - No authoritative Beta or Namecheap deployment has occurred.
deployment_dependency: The backend integration endpoint must later be promoted to and deployed from the authoritative Beta backend before live Drupal can consume real MERDPOS data.
updated_at: "2026-09-04T03:15:35+05:00"
next_action: Run final validators, commit the locally verified adapter checkpoint, archive this packet, push beta/drupal-webapp, and verify remote/local equality.
