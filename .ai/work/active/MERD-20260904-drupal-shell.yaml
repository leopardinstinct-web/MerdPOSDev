version: 1
id: MERD-20260904-drupal-shell
branch: beta/drupal-webapp
base_head: 7315be89ab1d940b20acbcf70598d6ea60e5056a
last_seen_head: 106b4ec61f091504d02da0fdf9daed313d33ece7
objective: Build the first Git-reconstructable MERDPOS Drupal shell with canonical branding, named permissions, system-role scaffolding, and a dashboard route.
acceptance_criteria:
  - Custom merdpos_core module is versioned under drupal/web/modules/custom.
  - Drupal consumes a synchronized copy of the canonical MERDPOS design tokens.
  - USER, ADMIN, SUPER, and DEV role scaffolds are installed from code/config, not manual-only state.
  - Protected MERDPOS dashboard route renders locally with no fabricated business data.
  - Existing MERDPOS beta runtime/database and Namecheap deployment remain untouched.
constraints:
  - GitHub remains source of truth.
  - Existing MERDPOS named-permission/LOA standard remains authoritative; Drupal roles are not a replacement for backend authorization.
  - DEV-only capabilities must remain explicit and restricted.
  - No secrets, SQLite database, settings.php, screenshots, or portable toolchain may enter Git.
owned_paths:
  - drupal/web/modules/custom/merdpos_core
  - drupal/tools
  - drupal/MERDPOS_DRUPAL.md
  - drupal/composer.json
  - .ai/work/active/MERD-20260904-drupal-shell.yaml
packet_state: VALIDATED
lifecycle: WIRED
completed:
  - Rebootstrapped against current GitHub namecheap-beta-live HEAD 7315be89.
  - Read AGENTS.md, .ai bootstrap/invariants/task-gates, canonical design tokens, authorization standard, and relevant Git history.
  - Implemented merdpos_core route, controller, theme hook, named permissions, role config, design-token synchronization, dashboard shell, and Drupal contract documentation.
  - Registered explicit Composer PSR-4 autoload for the custom module namespace and rebuilt autoloads.
  - Enabled merdpos_core locally and assigned the local admin account the MERDPOS DEV scaffold.
  - Verified protected anonymous denial and authenticated /merdpos HTTP 200 behavior.
  - Playwright smoke passed with Management workspace, MERDPOS DEV role label, operational-DB safety marker, and no fabricated currency values.
  - Rendered desktop screenshot inspected: MERDPOS content is balanced and unclipped; generic Drupal outer chrome remains intentionally for the next milestone.
checks:
  - PHP lint passed for merdpos_core.module, DashboardController.php, and sync_merdpos_design_tokens.php.
  - Design-token --check matched the canonical Beta token file exactly.
  - composer.json validated and Composer autoload generation succeeded.
  - Drupal cache rebuild and bootstrap succeeded.
remaining:
  - Commit and push validated source to beta/drupal-webapp.
  - Replace generic Drupal outer chrome with a MERDPOS-owned application theme/shell in the next milestone.
next_action: Commit and push this validated foundation, then make the MERDPOS app theme the next isolated step.
updated_at: "2026-09-04T02:17:00+05:00"
