id: MERD-20260906-attendance-qr-e2e
branch: milestone/attendance-qr-e2e-v1
base_head: 97be696366fd7fb2da9c078fcd34c79513983088
last_seen_head: 97be696366fd7fb2da9c078fcd34c79513983088
objective: Prove the current Beta attendance QR workflow end-to-end using disposable DUMMY context, without mutating production staff attendance.
packet_state: CODED
lifecycle: CODED
scope:
  - Generate/use a valid MERDPOS attendance QR in an exact DUMMY employee/store/device context.
  - Verify Home My current shift QR action clocks IN and OUT through the authoritative attendance service.
  - Verify Working Now and Timesheet/Reports reflect the attendance lifecycle.
  - Verify duplicate/cooldown, invalid/expired QR, wrong-store and unauthorized paths.
  - Verify forgot-previous-user/POS handover creates the expected governed dispute.
  - Verify dispute review resolution preserves frozen payroll/timesheet reconciliation rules.
constraints:
  - DUMMY-only destructive/attendance mutation testing; abort if exact DUMMY identity/context is not proven.
  - No direct Drupal writes to operational attendance tables.
  - No Flutter/full-POS M3.x, Checkout & Tender or retail-sale scope in this Beta milestone.
acceptance:
  - Valid DUMMY clock IN and OUT observed end-to-end.
  - Current Shift, Working Now and Timesheet/Reports agree with authoritative backend state.
  - Protection/error paths are exercised without production mutations.
  - Handover/dispute path is exercised and resolved through canonical services.
  - DUMMY test evidence/data is cleaned or explicitly retained as labelled evidence.
checks:
  - name: Fixture/browser harness syntax
    result: PASS
    evidence: PHP lint, Node syntax and git diff check pass locally.
remaining:
  - Deploy the DUMMY-only fixture/browser harness and execute the live acceptance sequence.
next_action: Deploy the DUMMY-only attendance fixture/test harness, prepare an exact DUMMY run, then execute the live Drupal IN/OUT and handover acceptance sequence.
blocked: false
blocker: null
updated_at: "2026-09-06T16:35:00+05:00"
