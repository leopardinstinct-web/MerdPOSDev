# Admin v1 changelog

Added a Blue Ice browser admin interface at `/admin/` using the existing employees table for authentication. Access is restricted to active employees whose `role_name` or `employee_type` is `ADMIN`. Added retail master-data, stock, supplier, purchase-order, report and audit schemas.
