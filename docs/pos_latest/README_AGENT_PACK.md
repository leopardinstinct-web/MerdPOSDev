# POS LATEST Agent Pack

Copy these files into the root of the `MerdPOSDev` repository, except where an existing project structure intentionally stores them elsewhere.

Recommended first action:
1. Review diffs before overwriting similarly named files.
2. Commit this documentation update on a documentation-only branch.
3. Ask OpenClaw to read `AGENT_AUTONOMY_CHARTER.md` and the listed source-of-truth files.
