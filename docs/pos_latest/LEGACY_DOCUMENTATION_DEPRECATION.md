# Legacy Documentation Deprecation Notice

The documentation under:

`markdown/pos_project_md_sources_updated_2026-06-14/`

and its ZIP archive is historical and **not authoritative** for current POS
LATEST development. It may be consulted only as project history.

Current guidance lives in `docs/pos_latest/`. Current implementation behavior
must always be confirmed from the checked-out GitHub source. If legacy text
conflicts with `docs/pos_latest/` or source, ignore the legacy text.

The older root-level Retail/Admin v1 installation and changelog files under
`docs/` are also historical implementation records. Relevant current facts
have been incorporated into `PROJECT_CONTEXT.md`, `FILE_MANIFEST.md`,
`API_CONTRACT.md`, `IMPLEMENTATION_STATUS.md`, and the roadmap draft.
